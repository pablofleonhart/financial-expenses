query GetTransactions {
  transactions(first: 100, orderBy: createdAt_DESC) {
    id
    amountSent
    amountReceived
    date
    description
    source {
      id
    }
    target {
      id
    }
  }
}
