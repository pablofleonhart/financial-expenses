query GetRevenues {
  revenues(first: 100, orderBy: date_DESC) {
    id
    amount
    bank
    date
    deleted
    description
    type
    currency
    itemStatus
    payment {
      id
      amount
      broker
      deleted
      currency
      holder
      available
    }
  }
}
